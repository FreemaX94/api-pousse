// setup.js — configuration globale avant chaque test

// Exemple : augmenter le timeout global des tests si nécessaire
jest.setTimeout(10000);

// Tu peux aussi ajouter ici des mocks ou initialisations globales
// Par exemple :
// global.someMock = jest.fn();
